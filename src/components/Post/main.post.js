import React from "react"
import Collection from "./collection.post"

const Post = (props) => {
    return (
      <React.Fragment>
        <Collection />
      </React.Fragment>
    )
}

export default Post